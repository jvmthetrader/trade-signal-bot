# Copy this file to deploy/.env on the VM and fill in your real values.
# deploy/.env is git-ignored and chmod 600 (private) — never commit it.
TELEGRAM_BOT_TOKEN=put-your-bot-token-here
TELEGRAM_CHAT_ID=put-your-chat-id-here
